{
	"scripts": {
		"dev": "sptcd -d -w ./dev/web-ui/public -r ../dev.s",
		"build": "sptc ./dev/web-ui/dev.s?action=build",
		"build:vercel": "npm run build && node build-vercel"
	},
	"dependencies": {
		"ollama": "0.6.0",
		"sptc": "0.0.36"
	},
	"devDependencies": {
		"@babel/standalone": "^7.26.4",
		"less": "^4.3.0"
	}
}
